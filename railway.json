{
  "schema": "v2",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd secure-api-middleware && npm install"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd secure-api-middleware && npm start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
