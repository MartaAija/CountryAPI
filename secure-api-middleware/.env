# Environment configuration for the Secure Country API service
# Contains database credentials and security keys
DB_HOST=localhost
DB_USER=user
DB_PASSWORD=password123
DB_NAME=country_api_db
JWT_SECRET=your_jwt_secret